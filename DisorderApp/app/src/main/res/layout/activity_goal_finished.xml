<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="ur.disorderapp.GoalFinished">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="30sp"
        android:text="@string/congratulation_title"
        android:layout_centerHorizontal="true"
        android:id="@+id/finished_title"/>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/finished_title"
        android:layout_centerHorizontal="true"
        android:id="@+id/finished_text"
        android:layout_margin="20dp"
        android:text="You are seeing this page because you have accomplished your goal! Nice! Click the button below to continue"
        />

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/btn_continue"
        android:layout_below="@id/finished_text"
        android:id="@+id/finished_btn"
        android:layout_margin="20dp"/>


</RelativeLayout>
